# Screenshots

Drop Burp screenshots here and they'll show up in the main README:

- `decoded.png` — the **Decoded** tab: pill breadcrumb + folding protobuf tree (dark theme reads best).
- `rules.png` — the **PolyProto** Rules-manager tab.

Suggested: capture at 2× on a dark Burp theme, crop to the panel, keep under ~400 KB.
